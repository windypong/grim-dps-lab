# 계산 범위와 가정 — v3 BETA

## 결론부터

이 앱은 장비·스킬·별자리의 데이터 연결을 자동화한 독립적인 평균 피해 모델입니다. **전체 게임 시뮬레이터가 아니며, 표시값은 지원한 효과의 소계입니다.** 지원하지 않는 효과가 붙은 빌드에서는 더 높은 결과가 나올 수도, 조건과 중첩 근사 때문에 더 낮은 결과가 나올 수도 있습니다. 따라서 소계를 엄밀한 최소 피해 보장값으로 볼 수도 없습니다.

## 데이터에서 계산으로

1. 아이템 본체, 접사, 컴포넌트, 증강제의 활성 레코드를 읽습니다.
2. 무기 자체의 추가 피해와 전역 보너스, 방어구 부위별 방어구를 분리합니다.
3. 고유 세트 조각 수에 맞는 능력치·스킬 보너스·스킬 변경을 활성화합니다.
4. 직접 투자에 전체/직업/개별 스킬 레벨 보너스를 더하고 최종 상한을 적용합니다. 투자하지 않은 스킬은 보너스만으로 새로 배우지 않습니다. 변형 스킬은 일반 +모든 스킬로 올리지 않습니다.
5. 각 레벨의 원시 DBR 배열을 읽습니다. 배열이 상한보다 짧으면 마지막 유효 값을 사용하고, 정확한 배열이 없으면 누락을 진단합니다. 1/기본 최대/최종 최대의 세 지점 사이를 보간하지 않습니다.
6. 하위 강화와 스킬 전용 변경, 피해 전환, 활성 버프, 숙련도와 별자리를 합산합니다.
7. 적·전투 조건으로 기대 피해를 계산합니다. 미지원 항목을 결과와 함께 보고합니다.

## 지원 상태

| 계층 | 지원 | 한계 |
|---|---|---|
| 검색·레벨 수치 | 공개 DB 전체 목록을 읽는 경로, 장비/스킬 레벨 연결 | 실제 전체 목록의 최초 다운로드는 개발 환경에서 실패 |
| 배분 검사 | 두 직업, 포인트 한도, 독점 스킬, 그룹 선행, 별자리 경로·친화도 | 모든 게임 UI 숙련도 해금 규칙을 완전 검증하지 않음 |
| 기본/발동 공격 | 일반 타격·기본 공격 대체·무기 발동 공격 | 실제 동작 시간·투사체 충돌은 근사 |
| 패시브·버프 | 인식된 수치를 활성 상태에 합산 | 특수 종족·상태·조건 수치 일부 미지원 |
| 보조 공격·약화 | 레벨별 피해/저항 감소와 평균 회전 | 복합 폭발·파편·프레임·정확한 시전 스케줄 일부 제외 |
| 세트 | 고유 조각 수별 능력치/스킬 변경 | 세트로 생기는 모든 특수 행동을 재현하지 않음 |
| 발동 효과 | 재사용과 확률·연결 스킬의 타격 기회 | 정확한 랜덤 이벤트열·상관·치명타 스냅샷 제외 |
| 펫·변신 | 원시 레코드와 제외 사유 조회 | AI·소환 수·변신 공격 루프 DPS 제외 |
| 카덴스·충전·집중형 | 특별 분류/경고 | 전용 주기·충전·틱 엔진 미완성 |
| 방어 수치 | 저항·부위별 방어구·능력치 등 수치 합산 | 사망 확률·생존시간·실제 SR 난이도 시뮬레이션 아님 |
| 로그 | DPYes JSON/CSV 필드 매핑·대상 필터·이동 평균 | 내부 틱·일시정지·리셋 로직까지 동일하지 않음 |

## 계산식

### 스킬 레벨

직접 투자 > 0일 때:

`유효 레벨 = min(최종 상한, 직접 투자 + 전체 스킬 + 해당 직업 + 해당 스킬)`

실제 레벨 보너스 출처를 표시합니다. 최종 레벨만 입력하고 장비 보너스를 또 더하는 중복 계산은 자동 모드에서 하지 않습니다.

### 무기 발동 공격

합계가 100% 이하이면 개별 발동률을 그대로 쓰고 나머지를 기본 공격으로 둡니다. 합계가 넘으면 개별 확률을 전체 합계로 나눕니다. 발동 공격이 기본 공격을 대체하므로 두 피해를 별도로 중복 합산하지 않습니다.

`공격 직접 DPS = Σ(선택 확률 × 경감 후 피해) / Σ(선택 확률 × 동작 시간)`

그다음 사격 유지율과 보조 스킬 시전에 쓰는 시간을 적용합니다. 기본 쌍수의 25%/25%/50% 분포와 각 발동 사격의 동작을 구분합니다. 실제 공격 시간은 데이터와 일반 추정에 의존하며, 측정된 평균 공격 횟수로 바꿀 수 있습니다.

### 피해 전환

스킬 전용 전환 → 장비/버프의 전역 전환 → 해당 무기의 관통 비율 순서로 처리합니다. 이미 전환된 부분을 다시 전환하지 않습니다. 같은 단계에서 경쟁하는 전환 합계가 100%를 넘으면 정규화합니다.

스킬 트리에 붙은 해당 피해 증가가 전환 결과로 이어지는 효과를 포함합니다. 원소 추가 피해는 세 유형으로 나누고 원소 피해 증가율은 세 유형에 각각 적용합니다. 물리의 지속 피해를 관통 전환을 이유로 출혈로 바꾸지 않습니다.

### 저항 감소

서로 다른 출처의 `-Y% 저항`은 더하고, 같은 출처의 중복은 최댓값으로 취급합니다. `Z% reduced`와 `X reduced`는 각각 해당 계열의 최댓값을 사용합니다.

```text
R = 기본 저항 − 서로 다른 -Y% 감소량 합
최종 저항 = R × (1 − sign(R) × max(Z% reduced)/100) − max(X reduced)
```

양의 저항만 전제한 식을 음수 구간에 그대로 적용하지 않습니다. 회귀 검사에는 실제 공개 실험의 부호·적용 순서 예시를 포함했습니다. 원시 키는 `defensivePierce`의 음수 디버프, `offensiveTotalResistanceReductionAbsoluteMin` 등을 구분합니다. 플레이어 장비의 양의 저항을 적 디버프로 처리하지 않습니다.

확률 발동 저항 감소는 평균 유지율을 반영합니다. 실제 순간 저항은 시간에 따라 달라지므로, 평균 저항에서의 피해가 모든 비선형 상호작용을 정확하게 대체하지는 않습니다.

### 발동과 지속 피해

```text
λ = 발동 기회/초 × 발동 확률
예상 발동/초 ≈ λ / (1 + λ × 재사용 대기시간)
유지율 ≈ 1 − exp(−발동/초 × 지속시간)
```

이는 포아송 기회·평균 재사용 모델입니다. 정확한 게임 이벤트·특수 리셋·버프 동시 발생을 재현하지 않습니다. 저체력·피격 조건은 선택한 시나리오에만 반영합니다. 확률형 재사용 초기화는 단순 재사용 감소에 합산하지 않습니다.

지속 피해는 무기 공유 출처의 경쟁을 단순화하여 가장 큰 평균값을 사용하고, 서로 다른 스킬 출처를 합산합니다. 공격 횟수마다 무한 누적하지 않습니다. 실제 양손·스킬별 출처 판정, 최상위 치명타 스냅샷과 교체 틱은 완전하지 않습니다.

### 능력치

체격·교활·정신의 직접 1포인트는 8입니다. 숙련도·장비·별자리의 추가 수치와 증가율을 합산합니다. 공개 원시 `records/game/combatformulas.dbr`에서 확인되는 공격/방어 능력 공식은 명시된 키로만 실행합니다. 임의 문자열 `eval`은 사용하지 않습니다.

연결되지 않은 성장식은 참고 프로필과 진단을 사용합니다. 이는 실제 전체 캐릭터 창 능력치와의 일치 검증이 끝난 상태가 아닙니다. 요구치 위반도 실제 장비 활성 규칙을 완벽히 재현하는 것이 아니라 계산 유효성 경고로 취급합니다.

## DPYes와의 비교

DPYes의 실제 피해 경감 후 로그와 동일 대상·동일 구간으로 비교해야 합니다. 다중 대상 최고 5초 DPS를 단일 보스 지속 DPS로 사용하지 않습니다. 예측을 실측 로그에 맞추기 위해 임의 상수를 자동 조절하지 않습니다. 장비와 스킬이 연결됐다는 이유만으로 수십만·수백만의 DPYes 성능을 보장하지 않습니다.

## 출처

- 공식 전투 가이드: https://www.grimdawn.com/guide/gameplay/combat/
- 게임 데이터 스키마: https://raw.githubusercontent.com/tednaleid/grimdawn-devotions/main/docs/item-schema.md
- 배포 방식: https://raw.githubusercontent.com/tednaleid/grimdawn-devotions/main/docs/deposit.md
- 저항 감소 직접 실험: https://forums.crateentertainment.com/t/actual-resist-reduction-formula/47174
- 기본 공격 대체/WPS 설명: https://forums.crateentertainment.com/t/turbo-charge-your-auto-attacks-with-default-attack-replacers-and-wps/49376
- DPYes 제작자 설명: https://forums.crateentertainment.com/t/tool-dpyes-player-pet-dps-meter-misc-util/133378/571

게임 버전이 달라지면 원시 수치뿐 아니라 이 문서의 동작 가정도 재검증해야 합니다.
