# 실제 검사 결과 — Grim DPS Lab v3 BETA

## 판정

**자동 계산 구현·브라우저 기능 검사는 통과했습니다. 사용자가 요청한 ‘전체 실제 DB + Windows 실행을 끝까지 검증’은 완료하지 못했습니다.**

이 문서는 테스트 코드를 만들어 둔 항목과 실제 실행한 항목을 구분합니다. 첨부 보고서는 실행 결과에서 생성했습니다. 테스트 개수는 실제 게임의 지원률·정확도 비율을 뜻하지 않습니다.

## 실제 실행한 검사

| 구분 | 환경·입력 | 결과 | 기록 |
|---|---|---|---|
| 새 자동 계산 엔진 | Node 22.16 / 합성 레코드·수학 예시 | **68 통과, 0 실패** | `tests/reports/auto-unit.json` |
| 백엔드·로컬 HTTP | Python 3.13.5 / 합성 DB 테이블·임시 서버 | **27 통과, 0 실패** | `tests/reports/backend.json` |
| 화면 통합 | Linux Chromium / 명시된 검사 표본 | **39 통과, 0 실패** | `tests/reports/ui.json` |
| 기존 수동 계산 회귀 | Node / 고정 계산 입력 | **48 통과** | `tests/reports/legacy-engine-console.txt` |
| 기존 카탈로그 회귀 | Node / 합성 카탈로그 | **20 통과** | `tests/reports/legacy-catalog-console.txt` |

합계 **202개 통과**는 위 서로 다른 검사의 합입니다. 범위가 겹치는 회귀 검사도 있으며, 202개의 실제 게임 빌드를 검증했다는 의미가 아닙니다.

### 자동 계산에서 확인한 예

- 비선형 중간 스킬 레벨을 원시 배열로 읽는지.
- 장비 +전체/+직업/+스킬, 미투자·변형 스킬·상한 처리.
- 수동 기본 능력치를 자동 모드에서 중복 합산하지 않는지.
- 기본 공격 대체와 발동 사격 계수, 100% 초과 정규화, 양손 로컬 수치.
- 물리·원소 전환 순서, 같은 부분의 재전환 방지, 전환 후 스킬 피해 보너스.
- 같은 세트 아이템 두 개를 서로 다른 조각으로 세지 않는지.
- 별자리 친화도·부분 투자·선행 별·배분 포인트.
- 저항 감소의 실제 원시 필드·양수/음수 적용 순서 예시.
- 발동 버프, 적 약화와 플레이어 저항 구분, 미지원 펫의 누락 경고.
- 확률형 재사용 초기화를 일반 재사용 감소에 더하지 않는지.

### 화면 검사 방식의 제한

개발 환경의 Chromium은 localhost 주소로 직접 이동할 때 `ERR_BLOCKED_BY_ADMINISTRATOR`를 반환했습니다. 그 제한을 우회하는 설정은 사용하지 않았습니다.

대신 **실제 로컬 서버에 Python HTTP로 접속해 받은 HTML을 Chromium `set_content()`에 넣어** 장비 검색·장착·배분 변경·분석·JSON 저장/복원·DB 포함 HTML·모바일 화면을 시험했습니다.

따라서 화면 기능과 로컬 서버 응답을 확인했지만, **브라우저의 실제 Windows localhost 탐색이나 기본 브라우저 자동 실행을 검증한 것은 아닙니다.**

오프라인 HTML도 이 검사에서는 생성 파일을 읽어 브라우저에 주입하는 방식으로 다시 열었습니다. 실제 Windows `file://` 내비게이션은 별도 미실행 검사에 포함되어 있습니다.

## 시도했으나 성공하지 못한 검사

`tests/reports/environment.json`에 실제 연결 시도의 오류를 보관했습니다.

- 공개 DB 배포 목록 URL: DNS 오류.
- 공식 Python Windows 임베디드 ZIP URL: DNS 오류.
- PyPI DuckDB 메타데이터 URL: DNS 오류.
- 별도 다운로드 도구로 배포 목록을 받는 시도도 실패.

대표 오류: `URLError(gaierror(-3, 'Temporary failure in name resolution'))`.

문서·웹 검색으로 데이터 스키마와 공식 설명을 읽을 수 있었던 것과, 실행 컨테이너에서 전체 Parquet 파일을 내려받아 DuckDB로 실제 변환한 것은 다릅니다. **후자는 실행되지 않았습니다.** 모의 다운로드 검사에서 성공한 사실을 실제 외부 연결 성공으로 표시하지 않았습니다.

## 실행하지 못한 검사

| 항목 | 상태 | 이유 |
|---|---|---|
| 실제 전체 DB의 모든 레코드 변환·검색 | **미검증** | 실행 환경 외부 다운로드 실패 |
| Python/DuckDB Windows 런타임 설치 | **미실행** | Windows 런타임 없음 |
| 네이티브 CMD·PowerShell 런처 | **미실행** | Linux 환경, cmd/PowerShell/Wine/QEMU 없음 |
| Windows 브라우저 자동 열기·보안 경고 | **미검증** | 실제 Windows 환경 없음 |
| Windows 실제 전체 DB E2E | **not_run** | `tests/windows_e2e.py`가 비Windows에서 검증을 거부 |
| 게임/DPYes와 동일 캐릭터 비교 | **미실행** | 게임 실행·실측 로그 없음 |
| 모든 스킬·펫·변신의 정확한 DPS | **미완성** | 전용 행동 모델 필요 |

## 첨부 이미지

`preview/*-test-only.png`는 **기능 검사 표본**의 실제 브라우저 화면입니다. 13개 아이템·25개 스킬·13개 별자리의 합성 레코드를 사용했습니다. 화면의 DPS를 서약운반자나 실제 게임 캐릭터의 DPS로 인용해서는 안 됩니다.

## 재현

로컬 검사 명령과 Windows 별도 실행 방법은 README 및 `WINDOWS_TEST_KO.md`에 있습니다. Windows GitHub Actions 파일은 준비했지만 이 대화에서 승인된 저장소로 실행되지 않았습니다. 네이티브 결과가 생기기 전까지 배포 상태는 BETA/미검증을 유지합니다.
